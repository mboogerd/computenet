package civictech.oracle.corpus

/**
 * Reserved for the pinned regression corpus — `PinnedSeeds`, the checked-in seed list and its
 * loader (epic computenet-4ru §2.2, `[ORA1-REPRO-01..03]`). Append-only by policy: AGENTS.md
 * forbids replacing a discovered failing seed with a friendlier one.
 *
 * **Owned by computenet-4ru.12**, not by this module skeleton. It holds no logic,
 * deliberately.
 *
 * Delete this file in the change that fills the package.
 */
@Suppress("unused")
internal object Placeholder
