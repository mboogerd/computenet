package civictech.oracle.run

/**
 * Reserved for the differential runner — `DifferentialRunner`, `RunOutcome`, `Mismatch`,
 * `WavePrefixOracle`, and the fold of each terminal through `SetView`/`MapView`/`CountView`
 * (epic computenet-4ru §2.2, `[ORA1-DIFF-01..11]`).
 *
 * **Owned by computenet-4ru.8**, not by this module skeleton. It holds no logic,
 * deliberately.
 *
 * Delete this file in the change that fills the package.
 */
@Suppress("unused")
internal object Placeholder
