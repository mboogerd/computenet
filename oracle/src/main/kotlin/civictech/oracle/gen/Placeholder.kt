package civictech.oracle.gen

/**
 * Reserved for the shape-typed deterministic case generator — `GraphGenerator`,
 * `ScriptGenerator`, `GeneratorConfig`, `Vocabulary`, `GeneratedCase` (epic computenet-4ru
 * §2.2, `[ORA1-GEN-01..10]`).
 *
 * **Owned by computenet-4ru.6**, not by this module skeleton. The package exists now so the
 * layout the epic specifies is real rather than promised, and so the catalog's shape rule
 * has a named consumer to be a contract *with* ([civictech.oracle.bind.ShapeRule]). It holds no logic, deliberately: guessing at
 * a generator here would be a second design of the thing 4ru.6 has to design once.
 *
 * Delete this file in the change that fills the package.
 */
@Suppress("unused")
internal object Placeholder
