# 91 — Consolidated Gap and Conflict Analysis

> **Status**: Living document — every `⚠` marker in the spec, in one table
> Conflicts (C-x) are places where ADRs and/or code disagree; each has a decision here or in the referenced section. Gaps (G-x) are missing designs or implementations, with proposals.

## Conflicts

| ID | Conflict | Resolution | Where |
|---|---|---|---|
| C-1 | Terminology drift: Task / Computelet / Cell | **Cell** canonical; others historical | 00/03 |
| C-2 | `Runner` vs `Host` split in germ (`ManagedRunner` duplicates a weaker `ManagedHost`) | **Resolved**: `ManagedRunner` was already gone from the tree; one `ManagedHost` + injected `HostScheduler` (`VirtualThreadScheduler` / `SimulationController` = the deterministic test host) | 00/03, 30/31, 50/52 |
| C-3 | `use { }` lambda API (Task Connectivity) vs serializable invocations (ADR 3) — code has both | `.call`/Invocation is canonical & the only wire form; `use {}` = local-only sugar for lease scoping and future context binding | 10/14 |
| C-4 | Legacy color runtime (`runtime.blocking/suspending`, PureTask/BlockingTask/SuspendingTask) vs germ model — two disconnected generations | **Resolved (M3.1)**: color model implemented on the kernel (G-3); legacy deleted (G-1) | 30/32 |
| C-5 | Reflection everywhere (`Invocation.of(Method)`, JDK proxies, `findPort`) vs ADR 3's no-reflection-on-wire + KSP direction | Reflection OK in-process short-term; stable ids on the wire; KSP proxies + port registry replace it incrementally. **M5.1**: stable ids landed — `@Contract` → generated `ContractDescriptor` tables, `Invocation.contractId/methodId` at capture. **Resolved (W4.6)**: every `@Contract` interface gets a KSP-generated proxy class (`gen.wire.ProxyRegistry`), replacing `java.lang.reflect.Proxy.newProxyInstance` for in-process cell API dispatch; `HostedCellProxy`/`HostProxy`'s cross-host structural navigation proxies (arbitrary `Cell`/`Port` types, not a fixed contract) remain on the dynamic-proxy fallback, out of this item's scope | 10/14, 10/15 |
| C-6 | Two port declaration styles (`by input()` delegates vs explicit `FanInlet.create`) | **Resolved**: both feed the per-cell `PortRegistry` (delegates via `provideDelegate`, explicit via `registerPort`); delegates preferred for hosted cells | 10/11, 10/12 |
| C-7 | Logic in constructors (`SetCell`, `TrafficLightCell` serve in `init`) vs `onActivate` rule | **Resolved (93 I-26)**: the layered-port model — structural admission is valid from construction, behavioral activation from `onActivate`; "Eager" is a KSP-checked capability (pure, allocation-free, host-context-free constructor handlers) from which unhosted-linking permission derives (enforcement surface → G-55); hosted default stays `onActivate` | 10/15 |
| C-8 | Per-link FIFO required vs `PriorityBlockingQueue` unordered ties in `ManagedHost` | **Resolved (tiebreaker)**: host queue orders by `(priority, sequence)`; sleep-based tests → deterministic host still pending | 30/31, 50/52 |
| C-9 | M10 journal replay re-drives effectful sinks un-suppressed (replay-stable identity makes state safe, but effects re-fire) | Decided rule ([93 I-7](93-feature-interactions.md)): `Effectful` sinks journal a processed frontier and replay suppresses re-driving them; **code diverges; fix pending** | 20/24, 30/31 |
| C-11 | `Shadow.spawn`'s plain NoOp proxy drops Owned/Leased payloads without `take()`/`release()` | Decided rule (93 I-20): shadow suppression of exclusive-carrying contracts is a discharging sink — Owned taken, Leased released, obligations discharged exactly once; **code diverges; fix pending** | 50/52, 20/23 |
| C-12 | ~~Landed RESTART restores the spawn-time checkpoint and continues emitting under the same outlet sourceId/counter (tag/wave aliasing possible)~~ — the divergence claim was **stale**, transcribed from the M3.5 prose rather than the code (adjudicated D-C12) | **Resolved (W2.1 core, adjudicated + conformance-covered D-C12)**: the supervision RESTART branch bumps the host-held generation, mints a fresh per-epoch `sourceId` on every outlet (collecting the superseded ids), restores the checkpoint, and emits the `ReBaseline(supersedes, supersede = true)` over the ordinary catch-up path; `TagState.applyReBaseline` drops the un-reasserted tags of the superseded sources and fences them as dead lanes. Covered by concord `21-REBASE-01` over a `rebaseline-source` restarted mid-stream against a delta-only twin. **Residuals unchanged, and NOT closed by this row**: the freshest-checkpoint tiers (landed restore = the local supervision-time checkpoint, I-22's own degenerate case) and the pull-merge direction (`supersede` always `true`) → G-43 + 93 I-25; epoch/generation reclamation → G-42 | 30/31, 20/22 |
| C-13 | `LocationRegistry` (`cell/host/LocationRegistry.kt`) co-locates three off-mandate distribution lanes beyond its stated job (where a ref lives + parking): the interest-assignment table (`interests`/`setInterest`/`interestOf`, CP-D2/CP-D3, ~L59-67), the `byLogicalId` membership index (PN-7 perf cliff fix, ~L41-48), and repartition hold/release (the flip-window buffer, 93 I-19, ~L224-241) — an undocumented accretion four subsystems (replication, partitioning, attention scatter, promotion) edit blind, with no marker recording the scope creep | No behavior change proposed here — each lane is independently correct and documented in its own KDoc. **Extraction target (deferred)**: split into `LocationRegistry` (location + parking only) + `InstanceIndex` (the `byLogicalId`/interest-assignment membership surface, CP-D2/CP-D3) + `DeliveryHold` (the repartition flip-window buffer, 93 I-19) once a second consumer needs one lane without the other two — reference-convenience justifies the current co-location, not a designed boundary | 30/33, 40/41, 40/42, 20/24 |

## Gaps — kernel & model (10)

| ID | Gap | Proposal | Where |
|---|---|---|---|
| G-8 | `CellRef` = bare UUID: no logical-vs-instance identity | **Resolved (M7.1)**: `CellRef(id, instanceId)`; wire VERSION 2; logicalId-binding realized per use-case (gossip mesh M7.3, promotion swap M9); logical rendezvous/instance-selection policy over `instancesOf` design decided (93 I-2 → G-57) | 10/11 |
| G-9 | No organelle port hiding/exposure | Design decided (93 I-10): exposure map with Flatten/Mediate surface modes over the TrafficLight idiom; implementation open (realization edges → G-52) | 10/11 |
| G-10 | Membranes have no code form | Realize as: handshake hooks → port policies → membrane object (cross-port rules). Membrane object design decided (93 I-10): exposure map + Flatten/Mediate surfaces + Symport/Antiport couplings; implementation open (→ G-52; coupling liveness → G-53) | 10/11 |
| G-11 | No data-path vs management contract distinction | **Resolved (M5.1 + M9.1)**: `@Contract(management)` on every contract + KSP push-only lint fails compilation on non-Unit data-contract returns; effect classification = `Effectful` marker (G-32) | 10/12 |
| G-12 | No Link object, no handshake, no unlink, no cardinality enforcement | **Phase 1 done**: `Link`/`LinkResult`/`onLink`/`onUnlink`/`unlink()`, cardinality → `Rejected`, `connect` surfaces results; cross-host = `Deferred` + dead-letter until wire (M5); ownership enforcement waits on G-21 markers | 10/13 |
| G-13 | No multiplexed ports / generic protocol stacking | **Minimal form done (M6.1)**: `ProtocolSupport` sub-channels keyed by `ProtocolId` on any port, links carry in-process endpoints; attention + suspension notices ride it. Remaining items closed by decision (93): data sub-ports rejected (I-1); wire crossing = PORT_PROTOCOL frames (I-1 → G-35); state-request folded into the plane as an upstream request answered by ordinary state-as-delta (I-16 → G-37), with frontier traversal/termination decided under the metadata plane (I-23); protocols against cold/suspended cells = idempotent drop (I-26). Implementation open | 10/12 |
| G-14 | No policy representation | **Phase 1 done**: composable link-time `LinkPolicy` on ports, first-rejection-wins; `LinkRequest.identity` slot exists (marker only, G-29); flow-time policies await membranes | 10/13, 40/43 |
| G-16 | No `onDeactivate` lifecycle hook | **Resolved (M3.3)**: hook + `despawn` + live-ref spawn guard; the drain protocol guarantees deactivation follows the flushed queue and captures state at that point | 10/15, 30/33 |
| G-17 | Port discovery via reflection walk | **Resolved**: per-cell `PortRegistry` fed by delegates (`provideDelegate`) and `registerPort`; reflective `findPort` deleted | 10/15 |
| G-35 | Generic protocols (PORT_PROTOCOL) cannot cross the wire and peers cannot negotiate or version each other's protocol capability sets — attention, saturation, state-request, and taps all stop at a bridge | Bridge egress/ingress gain a PORT_PROTOCOL frame path (one WireFrame type variant, direction tag, reverse-channel realization for upstream protocols over the reverse bridge path a cross-host link already maintains); the link's negotiated protocolCapabilities generalizes to cross-peer negotiation with a versioned ProtocolId↔contractId mapping and downstream-only capability sets for shadow taps; verified by a generative frame-reorder/duplication harness (cross-host attention convergence as the first case) (93 I-1/I-4/I-17/I-9) | 10/12, 40/41, 30/34 |
| G-48 | No cross-host index enumerates all links pointing at a full ref, so promotion swaps cannot find their relink set, shadow cuts cannot compute SCC closure, and logical-level orchestration is O(everything) | A maintained cross-host reverse-topology index (inbound and outbound links per full ref, including bridged links) with defined maintenance/migration cost, serving the promotion swap set, SCC computation for observation-membrane cuts, and instancesOf-based orchestration (93 I-2/I-11/I-17) | 10/11, 40/41, 50/53 |
| G-52 | The adopted membrane design (exposure map + Flatten/Mediate surface modes over the TrafficLight idiom) is undesigned at its edges: DSL lowering and proxy generation, exposed-name resolution across the wire, nested/transitive exposure, wave re-mint interplay, and leaf-cell membranes | KSP-generate the Mediate proxy (Invocation capture + policy/coupling/re-mint) from the membrane declaration and lower the DSL to spawn/serve/delegate/connect; announcements carry exposed aliases only, resolved bridge-side to organelle full-ref ports without leaking the interior; define exposure re-composition (wave-scope/mode) across nesting levels with cardinality accounting; pin Remint interaction with attention propagation and late-join catch-up; and specify the minimal non-composite (leaf) membrane form (93 I-10) | 10/11, 40/41, 50/51 |
| G-53 | Cross-port couplings (Symport/Antiport) can wait forever when one coupled port never fires for a wave, and the fate of a half-completed coupled transaction caught in a promotion-swap buffering window is undefined | A timeout/veto/abort policy for stalled couplings that composes with no-message-loss and the drain protocol, plus ordering semantics for coupled transactions across a buffered swap window (93 I-10/I-11) | 10/11, 30/33, 50/53 |
| G-55 | The admission (structural, from construction) vs activation (behavioral, handler-establishment) split needs its enforcement surface: stateful-onLink classification, deferred-admission result surfacing including cross-host, Eager verification, dropped-protocol observability, and remote-spawn rejection channels | A per-port structural-only vs stateful onLink declaration with defined defer/replay/result-surfacing of admission requests to not-yet-hot cells, composing with LinkResult.Deferred and registry park/replay across the wire; a KSP-checked Eager capability (handler in constructor, pure, allocation-free, host-context-free) from which unhosted-linking permission derives; a count/log policy for protocols dropped before handler install; and a typed rejection surface for wrong-color or invalid remote spawns pinned against G-26/G-12 (93 I-26/I-15) | 10/15, 10/13, 40/41 |
| G-57 | A client holding only a logicalId has no defined instance-selection policy (nearest replica for reads, leader for writes, active candidate during promotion), and instanceId minting has no stated collision discipline across hosts or in deterministic tests | A logical rendezvous policy over instancesOf keyed by operation class; the port-compatibility rule on relink (reject vs adapt on (portName, contractId) mismatch) confirmed to live on ports rather than refs; and a stated birthday-bound argument for random instanceId minting with caller-chosen ids in deterministic tests, covering construction-time NewInstanceOf minting across hosts (93 I-2/I-21) | 10/11, 40/41, 50/51 |
| G-60 | **Partially landed**: the descriptor sweep now emits per-cell PortDescriptor rows (name, direction, contract id) into CellDescriptor, generates typed `<Cell>Ports` id objects, and enforces the registry-name==property-name half at spawn. Remaining bits: protocol lints, effect-boundary flag, size() well-formedness, Eager, determinism, pull-safety, fallback-tier markers. Original: a dozen adopted mechanisms hang on undesigned KSP descriptor bits and lints: protocol descriptors + registry, the ownership-free/idempotence protocol lint, color, the effect-boundary flag with opaque-effect detection, @Key, magnitude/idempotentMerge bits, size() well-formedness, Eager, determinism, pull-safety, and fallback-tier markers | One descriptor-generation sweep extending the M5.6 exclusive-bit scan: emit the per-type/per-contract bits into CellDescriptor/ContractDescriptor/ProtocolDescriptor with stable cross-peer ids, and fail compilation on the associated violations (Owned/Leased in generic-protocol contracts, non-null-context expectations, broadcast-keyed exclusives, opaque I/O outside effect boundaries, non-eager constructor handlers, catch-up fallback on non-idempotent cells) (93 I-1/I-5/I-6/I-7/I-8/I-15/I-16/I-17/I-26/I-27) | 10/12, 10/15, 20/23 |
| G-63 | The kernel package graph does not realize the layering `02-design-principles.md` claims: all 20 non-leaf `civictech.cell.*` packages form **one strongly-connected component** (SCC) — no package is import-reachable-only-downward from another — while P1+P2's "How principles interact" note reads "kernel (logic) → host (queues, colors) → distribution (proxies, replication)" as a physical dependency layering. No `G-*`/`C-*` recorded the divergence before this entry; `02-design-principles.md` now points here instead of asserting the layering as built | Not a redesign — a physical layering this entangled would fight P3 (explicit links) and P9 (serializable boundaries), which do not respect package layers. Two cheap first cuts were cited as the remediation sketch rather than a committed plan: (1) **done** — `AttentionPolicy` moved from `.host` to `.control` (commit `49fef6e`, T11-C), so the scheduling package no longer needs a back-reference from `.host`; (2) still proposed — invert the `.host → .wire` direction behind a small codec interface owned by `.host`, with `.wire` providing the implementation, so hosting stops importing the transport package it is conceptually below. Cut (1) alone does not collapse the SCC to a DAG; a full physical layering is out of scope here (→ 95, research-gated) | 00/02, 10/11 |
| G-65 | `12-ports.md` §Inlet policy tiers specifies a **GATE** tier (FIFO hold for suspension/backpressure) as one of four chain tiers; `PolicyTier.GATE` and its `Gate` policy had zero production installs (only a definition-only test) and were deleted (remediation T03) — real backpressure lives in `IntakeControl`/`ParkQueue`, outside the tier-chain abstraction | Not a redesign — the tier-chain vocabulary (ADMIT/ALIGN/ACTIVATE) stays exactly as landed. A GATE tier can be reintroduced with its first real user once a concrete case needs FIFO-hold semantics expressed as an inlet-policy tier rather than through `ParkQueue` directly | 10/12 |
| G-64 | `ProtocolSupport`/`PortRegistry` key ports in **JVM-global** maps whose handler-closure values reference their keys, so ports created across an entire test suite accumulate rather than being scoped per host/registry instance — the only mitigation is `buildSrc/src/main/kotlin/kotlin-jvm.gradle.kts`'s `setForkEvery(80)` (periodically restarting the test JVM) plus a raised `maxHeapSize`, both acknowledged only in a build-script comment and tracked nowhere in the spec | End state: instance-scoped `ProtocolSupport`/`PortRegistry` (one registry per `ManagedHost`/test fixture, not a JVM-wide singleton), so port/protocol registration lifetime matches host lifetime and `forkEvery`/heap tuning becomes unnecessary rather than load-bearing. Until then, the build-script comment at `buildSrc/src/main/kotlin/kotlin-jvm.gradle.kts` (`forkEvery bounds accumulation...`) is the current mitigation and the citable source of truth for *why* the fork setting exists | 10/12, 10/15, 50/52 |

## Gaps — semantics (20)

| ID | Gap | Proposal | Where |
|---|---|---|---|
| G-4 | **No MessageContext on invocations** — blocks glitch-freedom, tracing, causal merges | **Resolved**: `Invocation.context` + `CurrentContext`; outlets stamp, `Invocation.invoke` restores; transparent inlet→outlet flow verified incl. cross-host | 10/14, 20/22 |
| G-18 | Pull/on-demand/late-join unspecified | **Partial (M4.2)**: late-join catch-up implemented via post-install `onLinked` (state-as-delta unicast, idempotent thanks to tags); on-demand pull now design-decided — `RequestState` on the metadata plane (93 I-16 → G-37), termination per 93 I-23 — build pending; catch-up wave-alignment decided via the baseline rule (93 I-24 → G-38) | 20/21 |
| G-19 | Cycle throttling unspecified | `Magnitude` on cycle deltas + quiescence thresholds. Design decided (93 I-5/I-6): wave re-entry = re-origination at declared CycleHeads (the wave-id+hop-count candidate demoted to a runtime liveness guard); divergence guard = descriptor bits + per-edge thresholds with a two-tier guarantee. Partially landed (M17): `cell.control.Magnitude` is code with two consumers — magnitude-band dispatch (34 decision 7) and `:agora`'s app-side head approximation (its topology-owning service designates cycle-closing edges as heads, threshold on the inbound feedback inlet; step-budgeted 100-seed exit test = the weak-tier empirical probe). Residual: kernel CycleHead machinery, weak-tier convergence rate, cross-host cycle detection, hop-bound calibration (→ G-41); fixpoint semantics open with a narrowed input (93 I-23). **Planned realization (residual)**: iteration context + fixpoint-cut mechanics 96 §E5 (weak-tier convergence stays 95 R2) | 20/21, 30/34 |
| G-20 | Wave-id assignment in a decentralized graph undesigned | **Decided & implemented**: `Timestamp(sourceId, counter)` minted per emitting outlet; convergence not simultaneity across sources; cycles open | 20/22 |
| G-21 | Ownership contracts unimplemented | Marker types → link-time enforcement (after G-12) → pools only when profiled. **M5.6: phases 1+2 done** — markers + KSP exclusive bit + enforcement at FanOutlet/Broadcast/bridge; phase 3 pooling awaits profiling. Read-only observation of exclusive flows decided via uncounted Borrowed taps (93 I-20 → G-47) | 20/23 |
| G-22 | Data cells can't serve state to late joiners | **Resolved (M4.2)**: every data cell unicasts state-as-delta-from-empty from `onLinked` and implements `Stateful` (drain/migrate round-trip verified); validated by a 100-seed late-join test with a missed-prefix control. Wave-alignment refined by decision (93 I-24): a catch-up snapshot is a topology-versioned baseline, not a wave input (→ G-38) | 20/24 |
| G-23 | Delta merges are arrival-order biased; not replica-stable | **Resolved for the set family (M4.1)**: observed-remove tags — `SetDelta` carries per-element `Timestamp` tag-sets minted cell-locally; merge = tag union (commutative, associative, idempotent), validated by a 200-seed convergence test with an order-biased control. `CounterCell` commutative by construction. Map/list carry documented convergence limits until replication (42). **Relational core convergent on the set family (M11)**: flatMap/map, semijoin/antijoin/difference, equi-join all batch-equivalent under the tag-hygiene rule (21); grouped aggregation converges by per-peer recompute (24). **OR-map keyed family landed (96 §E1.2-E1.3, E1-CORE + E1-REPL)**: `TaggedMapDelta<K,V>`/`OrMapCell` (local core, dot-shaped puts/dels, laws `[24-TMAP-01..04]`) plus `Replicable<TaggedMapDelta<K,V>>` (echo-terminating gossip, C-10 re-origination, pull baseline, `ReBaseline` dead-source fencing) — `kernel/.../cell/data/OrMapCell.kt`, `.../delta/TaggedMapDelta.kt`. Weighted/bag semantics still deferred, trigger unchanged (first EXCEPT ALL / weighted consumer; the idempotent construction is `PnCounterDelta` generalized; weighted family per-replica in 96 §E6, replicable bag stays research, 95 R17) | 20/24, 40/42 |
| G-24 | No partitioned state | **Deferred with trigger (M4; trigger status updated M11)**: PartitionedCell = composite cell + key-routing proxy; placement = ordinary placement. The first keyed dataset now exists (`GroupByCell`) and shards cleanly — a `SetDelta` splits by element via `hash(keyFn(e))`, and shard `MapDelta` outputs merge trivially (disjoint keyspaces preserve single-writer-per-key). Full design decided and unified (CP-D1): partitioning is the disjoint-interest setting of the instance-set mesh (40/42 §Interest-scoped instance sets) — the `PartitionedCell` composite (93 I-8) is a naming convenience over a disjoint-interest instance set, and every distribution edge is inherited from replication rather than re-earned (former G-56 residuals resolved-by-design there); live-placement composition decided (93 I-19); trigger unchanged: placement pressure on one instance. Realization: CP-D2 (`Interest` filter on the linker), CP-D3 (shards on the substrate), CP-D4 (repartition) | 20/24, 40/42 |
| G-25 | No durability | **Resolved (M10)**: write-ahead `Journal` at the host intake (wire frames — a journal is a bridge to disk), `recoverFrom` = checkpoint-restore + replay through the ordinary decode path; `checkpoint` compacts (tombstone/slot compaction rides it); replay-stable identity (ref-derived tags/slots, counter in snapshots). Remaining: disk-overflow mailbox (33), journal segmentation — trigger: first workload where one file hurts | 20/24, 30/31 |
| G-37 | On-demand pull (the G-18 residual) has a decided shape but no concrete design: descriptor, reply routing to a specific requester, buffer-survival detection, pull storms on mesh heal, and pull-safety for non-idempotent/effectful cells are unspecified | RequestState(replyTo, since) on the metadata plane answered by an ordinary state-as-delta single wave, issued by the subscriber exactly when a link goes live history-incomplete (fresh link → pull; parked-and-replayed → none; dropped-and-re-resolved → incremental pull with a TagFrontier under a stated per-source-monotonic tag invariant); add the per-link liveness epoch for park-vs-drop detection, a mesh-reconnect coalescing/debounce policy, and a pull-serves-copy-only rule for non-idempotent cells (93 I-16/I-1) | 20/21, 10/12, 40/41 |
| G-38 | A multi-source catch-up snapshot has no single wave id under per-source waves, and unwaved pass-through lets a mid-wave late joiner glitch — 20/22's 'stamped with the wave it represents' is unsatisfiable as written | Catch-up state-as-delta is a topology-versioned BASELINE (nullable MessageContext.baseline) causally anchored to the link-install event and excluded from wave-completeness; glitch-freedom resumes at the first wave complete over the post-install topology; tag frontiers are valid only for per-source-monotone families (full-state fallback otherwise); extend the diamond/late-join harnesses with mid-wave late-join, quiet-upstream transition-window, incremental-since, and multi-arm simultaneous-join cases (93 I-24/I-16) | 20/22, 20/21 |
| G-39 | Link/unlink are null-context management ops with no stamp in the wave domain: glitch-free consumers cannot know from which wave a new/removed edge counts, source-set changes do not propagate downstream, and EdgeEvents/floors have no wire form | In-band EdgeOpen/EdgeClose markers injected into the affected link's own FIFO carrying a per-source flushed-high-water floor; design the floor representation and retention/compaction horizon, hop-by-hop downstream source-set delta propagation with a liveness proof (an upstream cut must not strand a waiting join), bridged EdgeEvent frame types ordered against data across disconnect/park/replay, the floors×cycles×merge-tag interaction, and the explicit topology-serializing coordinator (JoinBarrier) cell that doubles as the diamond-over-replica escape hatch (93 I-13/I-14). **Planned realization (residual)**: the diamond-over-replica read as a delivered-watermark lattice, 96 §E3 (E3.4) | 20/22, 40/41, 20/21 |
| G-40 | A glitch-free join cannot distinguish an effective-only-silent arm from a dead one: wave completeness blocks forever on absorbing, suspended, restarting, or dead-lettered frontier edges | Per-source per-edge watermarks advanced by real deltas, by metadata-plane Progress absorb-acks emitted at a precisely defined quiescence boundary, or by later waves (monotone close); typed Stall(reason, recoverable) markers with per-edge WAIT \| DEGRADE \| RE-SCOPE policies keyed on recoverability; pin the DEGRADE correctness contract (how downstream distinguishes a degraded emission), calibrate the backstop deadline against frontier depth, and build the generative completeness harness (93 I-18). **Planned realization (residual)**: absorb-acks from operator cells 96 §E2 (E2.2, landed CP-A3/CP-A4 in-process and CP-A2 bridged, now normative at 20/22 §The observation frontier / §Completeness over silent or stuck edges, 96 §E2.1; `CombineLatestCell` and `LookupJoinCell` closed by E2-GATE, wave C2 — no cell in the absorb-ack-eligible set remains unacked); frontier-gated emission landed opt-in on `SemiJoinCell`/`CombineLatestCell` (`WaveGate`, E2-GATE, 96 §E2.4) and the aligned multi-view sink landed (`AlignedCompositeCell`/`observeAligned`, E2-ALIGN, 96 §E2.3 second half; see F-5); **new residual, found by `E2-SUITE`'s acceptance suite**: an *ungated* binary operator (`JoinSetCell`, and by the same shape `IntersectSetCell`/`JoinCell`) cannot participate in a wave-aligned composite — it reconciles per inlet invocation, so one wave can absorb-ack then emit under that same wave, tearing the aligned sink's release; `emitOnFrontier` extension to these cells is unscheduled (99-defects-engines-plan §CC3 residual note); cross-replica completeness 96 §E3 (E3.4) | 20/22, 30/34 |
| G-41 | The adopted CycleHead/threshold structure leaves admission and well-formedness holes: cross-host cycles are invisible to link-time checks, multi-head/nested/multi-tier cycles have no stated rule, hop bounds are uncalibrated magic numbers, and head behavior under RESTART/promotion is unpinned | Distributed cross-host cycle detection (or an explicit hop-guard-only stance) tied to peering/announcements; a ≥1-head-per-elementary-cycle well-formedness rule with detection over the cycle basis; a multi-tier mix admission policy; hop-bound calibration against loop diameter; feedback-join consistent-snapshot semantics; membrane-scoped lap quiescence; plus generation-bump (RESTART) and swap-on-live-cycle behavior at a head — weak-tier fixpoint convergence itself stays open under G-19 (93 I-5/I-6/I-17/I-22). **Planned realization (partial)**: cycle-region capture, ingress/egress classification, stratification admission 96 §E5 (E5.3, E5.6); cross-host/nested structure stays research (95 R16) | 20/21, 40/41 |
| G-42 | Epoch source-ids and restart generations accrete unboundedly: OR-set/PN source columns, stale glitch-free partial-wave buffers, and frontier entries for vanished epochs are never reclaimed, and counter/generation continuity across migration and host failure is unpinned | Safe reclamation of provably-superseded epochs (compaction riding G-25 checkpoints), frontier GC for orphaned partial waves triggered by relink-driven recompute, a concrete migration-payload field carrying the outlet counter high-water, durable-counter batching kept off the emission hot path, and generation derivation from the journal high-water (fresh high base on non-durable hosts) so post-restart tags never alias (93 I-14/I-22/I-3/I-7). **Planned realization (partial)**: causal-stability trigger + tag/tombstone reclamation 96 §E3 (E3.5, E3.7); event-time state eviction 96 §E4; epoch-column hygiene stays research (95 R14) | 20/22, 20/24 |
| G-46 | Exclusive (Owned/Leased) payloads have no defined story off the happy path: a payload parked-but-unsnapshotted at crash is lost with no stated at-most-once contract, and the DeadLetter envelope for freezing/serializing/redacting them is unspecified | State the sender-durability contract that makes crash loss at-most-once acceptable (or require the producing host to be durable), and pin the DeadLetter envelope: Owned → move-by-serialize at capture, Leased → released, with a redaction rule for non-serializable payloads — mergeable parked traffic is already covered end-to-end by the M10 journal + anti-entropy pair (93 I-7/I-22/I-12) | 20/23, 30/31, 20/24 |
| G-47 | The uncounted read-only Tap (a Borrowed projection fired before the sole consumer) that lets invariants/shadows/judges observe exclusive flows is adopted but unbuilt: projection derivation, catch-up, and copy-fork are open | KSP derives Borrowed-projected observer descriptors from exclusive-carrying contracts (nested/generic payloads, link-time validation that a tap's contract equals the outlet projection); taps on exclusive flows are attach-forward-only (no retained history to replay); a Cloneable/copy capability for Shadow.forkExclusive with a stated failure mode for uncloneable payloads and unspecified Leased forks (93 I-20) | 20/23, 50/52 |
| G-56 | ~~PartitionedCell's adopted design leaves its distribution edges open: routing-table epoch consistency, repartition-window buffering, bulk-rebalance atomicity, supervision-travels-with-placement, per-shard replica targeting, range queries, per-key attention routing~~ | **Resolved by design (CP-D1, superseded by 40/42 §Interest-scoped instance sets)**: partitioning has no partition-specific distribution edges — it is the disjoint-interest setting of the replication mesh and inherits its edges. Each former residual dissolves or relocates: routing-table epoch = the versioned interest-assignment table's `routingEpoch` (realization CP-D3); repartition-window buffering = the mesh's park/replay on interest reassignment (CP-D4); per-shard replica targeting = the overlapping-interest sharded-replication setting (not a mechanism); range read = the union of disjoint-key catch-ups. The genuinely-open remainders are not partition edges: supervision-follows-placement → **G-61**; bulk-rebalance / resharding triggers / per-key attention routing → the economic layer **G-62** | 20/24, 40/42 |
| G-59 | The M10 journal replays intake frames, which is sound only for deterministic, input-driven cells: wall-clock/random logic, spontaneously-emitting sources, Effectful sinks without idempotency keys, glitch-free partial-wave buffers, and cross-host recovery-frontier drift are unhandled | A determinism marker/lint forcing non-deterministic cells to output-mode journaling (or a captured-entropy WAL record); an emitted-delta log format for sources and a processed-frontier shape for Effectful sinks with a generative recovery-dedup test; document the external-idempotency ceiling as a stated limit; verify deterministic replay reconstructs partial-wave buffers or include them in Stateful.snapshot; and evaluate an opt-in coordinated checkpoint for tightly-coupled subgraphs (never global, per P4) (93 I-7) | 20/24, 30/31, 50/52 |

## Gaps — execution (30)

| ID | Gap | Proposal | Where |
|---|---|---|---|
| G-3 | Color model not in germ | **Resolved (M3.1)**: `HostColor` on `HostScheduler`, `BlockingCell`/`SuspendingCell` markers, spawn validation; bridges degenerate while intakes are unbounded (32). Cross-color composition design decided (93 I-15), core landed M3.1; color-keyed bridges re-enter at the saturation seam (→ G-34) | 30/32 |
| G-27 | No coroutine ManagedHost | **Resolved (M3.1)**: `CoroutineScheduler` written fresh (legacy suspending runtime was empty stubs); suspend-capable `SimulationController` stepping | 30/31, 30/32 |
| G-26 | Error handling = printStackTrace | **Resolved (residual)** (M3.5 + M4.4): `DeadLetter` on host `deadLetterOutlet` + per-cell supervision (PROPAGATE / RESTART / SUSPEND with `resume` replay) + opt-in per-cell error outlets (`ErrorReporting` marker; host emits `CellError` under every policy; consumed by invariant cells). RESTART contract redefined by decision (93 I-22): freshest-checkpoint restore + fresh per-epoch sourceId + generation-seeded ReBaseline supersession — the fresh-epoch + supersession core **landed** (W2.1; C-12 resolved, adjudicated in D-C12), the checkpoint-tier and direction residuals → G-43. **Residual**: four print-and-continue sites survive outside the DeadLetter/supervision path — `VirtualThreadScheduler.kt:33`, `CoroutineScheduler.kt:47` (`e.printStackTrace()`, uncaught scheduler-loop exceptions), `LocationRegistry.kt:144` (`notifyLink`), `LocationRegistry.kt:281` (`notifyPublish`) (`System.err.println(...)`, best-effort notification-hook failures, T02-F) | 30/31 |
| G-28 | No host hierarchy (quotas, cascade, placement) | **Resolved (M8.1)**: parent/child recorded at spawn; subtree cell quota walks ancestors; drain cascades children-first. Redirection/resource models remain | 30/31, 40/43 |
| G-5 | Mobility protocol unimplemented (closable queues, drain, location registry, state capture) | **Resolved (M3.2–M3.3)**: closable intake + fail-fast + `LocationRegistry` park/replay; two-phase `drainHost`/`resumeHost`/`migrate` with `Stateful` snapshot capture. Disk overflow mailbox remains with G-25 | 30/33 |
| G-6 | Attention/scheduling undesigned beyond static priorities | **Resolved (M6)**: `AttentionSupport` (max aggregation, band quantization = damping) over generic-protocol sub-channels; `ManagedHost`+`AttentionPolicy` band dispatch, stride floor, NONE-window park/replay; `GlitchFreeCell` WAIT/DEGRADE. 100-seed harness + starvation control. Remaining refined (93): retraction/merge/context closed by decision (I-4 → G-58); wire crossing designed via the metadata plane (I-1 → G-35), implementation open; WAIT/DEGRADE closed for all five non-delivery causes (I-18 → G-40); transitive/multi-hop notices still open (→ G-36) | 30/34 |
| G-34 | Intakes are unbounded — no saturation signal, no admission gate; the ADR-2 color bridges are degenerate and every parking bound (pre-activation park, router funnel, park-at-sender) is unenforceable | Three-state OPEN/SATURATED/CLOSED intake flag on the existing closure fast-path read; saturated sends dispatch by payload class (mergeable deltas coalesce into bounded per-source pending slots, exclusive/non-mergeable park in-order at the sender, management band exempt); SaturationSignal rides the metadata plane upstream with a terminal park-overflow policy (visible dead-letter default, Block(timeout) opt-in only, with Block × glitch-free-wave semantics pinned), realized at the enqueueHostedInvocation seam keyed by (senderHostColor, targetHostColor), including the cross-wire saturation frame vs transport flow control (93 I-12/I-15/I-9/I-19/I-26) | 30/32, 30/31, 10/13 |
| G-36 | All metadata-plane notices are single-hop (M6): attention retraction, upstream disinterest, Stall/Progress, and state-request pulls do not propagate through absorbing or stateless intermediaries to distant joins or remote producers | One hop-by-hop re-emission rule for metadata-plane notices with loop prevention and the band-quantization interaction pinned per protocol — attention levels and transitive retraction across damped hops, disinterest quiescing remote producer cones, stall/progress watermarks reaching deep joins, and requestState forwarding through stateless cells (93 I-1/I-4/I-9/I-16/I-18) | 30/34, 10/12, 20/22 |
| G-43 | RESTART's restore-freshest-checkpoint + generation-stamped re-baseline leaves precedence and cost open: supersede vs concurrent multi-source remove, re-baseline cost under wide fan-out, hybrid push/pull direction, poison-write loops, and the recovery-cell pattern are unstandardized | State a supersede-vs-remove precedence with a generative convergence test; bound the push-authoritative re-baseline (diff-against-last-acked / delta-since-generation); define the per-cell direction policy for hybrid derivation+owned-state cells; add a poison-write escape (dead-letter the replaying write after N RESTARTs); standardize the deadLetter→requestState recovery cell — replicated cells re-baseline from mesh peers, resolving the RESTART-within-replication question carried by four earlier challenges (93 I-22/I-2/I-7/I-18/I-19/I-25) | 30/31, 20/24, 40/42 |
| G-58 | The per-link LWW attention algebra leaves realization details open: version minting and wraparound, frontier state across migrate/relink, deadlineHint folding, retraction racing suspension-region atomicity, policy-window calibration, and band pinning for hard monitors | **Core resolved (W4.5)**: `VersionMinter` mints a per-emitter monotonic version, compared wraparound-safely via signed difference; `AttentionFrontier` applies incoming updates iff newer (idempotency: duplicate/stale absorbed, later version supersedes) and retracts/GCs the slot on `onUnlink`; `AttentionAggregator.decay`'s `cadenceTicks` knob quantizes decay recompute into tick buckets (defaults without theory — economic calibration is research, 95 §R6). Remaining: frontier state across migrate/relink, deadlineHint folding, retraction × atomic-park race, policy-window calibration, and band pinning for hard monitors (93 I-4/I-9) | 30/34 |
| G-61 | Nothing decides where cells land: the color-aware co-hosting engine (30/32 SHOULD), GraphSpec placement constraints, multi-host replay routing, spawn redirection (the G-28 remainder), and membrane co-location cost policies are all unbuilt | A placement engine consuming CellDescriptor.color and optional GraphSpec placement constraints to co-host same-colored chains, replicate pure cells per color neighbourhood, and route multi-host replays; realize M8.1's deferred spawn redirection as its enforcement hook; and a placement/cost policy bounding how much in-flight remote traffic a candidate co-location swap may park when membrane links span many peers (93 I-15/I-11/I-19) | 30/32, 30/31, 50/53 |
| G-62 | Every interest-driven policy defers to an economic layer that does not exist: replica spawn-vs-subscribe, migration-toward-attention, partition-host split/merge, resharding triggers, and per-Principal attention budgets (the Sybil economics) | An attention/quota-driven economic layer (the G-6 residual, on the G-28 quota walk) deciding when to spawn a local replica vs subscribe remotely and where replicas live, migration candidacy under persistent high attention with remote hotspots, partition split/merge and bulk-rebalance triggering by load/size/attention, and per-Principal resource budgets bounding authenticated interest claims with a concrete cost to mint an identity (93 I-3/I-9/I-19/I-8/I-28) | 30/34, 40/42, 40/43 |

## Gaps — distribution (40)

| ID | Gap | Proposal | Where |
|---|---|---|---|
| G-15 | No wire layer (format, transport, addressing) | **Resolved (M5)**: method-id tables + `ContractRegistry` (M5.1); `WireFrame` envelope — kotlinx JSON, ids-only, version byte (M5.2); loopback bridge cells + generative harness across two registries (M5.3); `Location = Local\|Remote` with announcements as ordinary wire traffic (M5.4); WebSocket transport in `:wire` (M5.5); exit: `DistributedCollaborativeAppTest` 100 seeds + two-OS-process demo (M5.7). **M10.3**: reconnect landed — capped-backoff client loop, re-hello catch-up, park-never-drop across the crash window. Remaining interim: JDK dynamic proxies in-process (C-5), security beyond allowlists (43) | 40/41 |
| G-7 | Replication undesigned | **Resolved for the mergeable class (M7 + delta 4)**: symmetric gossip-mesh linker (`Replication`, contract `Replicable`) over announcements; tombstoned OR-set + `PnCounterCell` (per-source pointwise-max) gossip via `deltaInlet` (echoes terminate on effective-only); anti-entropy = park/replay + idempotent catch-up. 3-peer 100-seed exit + divergence control; PN mesh/partition/late-join tests. Mesh skeleton design decided (93 I-3: topology rule, eviction gate, membership fold) and hardened (W3.3): the eviction gate (membership-gated suspend-when-partitioned + drain-gated despawn) and the replica-convergence invariant harness are built — G-45's liveness/churn argument residual only; single-writer leader-follower design decided (93 I-25 → G-44), residuals narrowed to automatic-election liveness/placement and durable-storage checkpointing; exclusive-payload cells confirmed replicable through their Frozen delta path (93 I-20); keyed structures remain (G-23) | 40/42 |
| G-29 | No threat model / identity | **Phase 1 done (M8.2–M8.3)**: `PeerId` from transport hello → ingress stamps deliveries → `LinkRequest.identity` (`CurrentPeer`); deny-by-default via `allowPeers` policy + ingress admission gate. Vocabulary + the three enforcement seams + attenuation + disclosure + integrity design-decided (93 I-28) and landed for in-process membranes (W4.1 → G-54 core); crypto authentication and encryption-at-rest remain; Sybil economics → G-62 | 40/43 |
| G-44 | Single-writer replication (leader→follower log-shipping) defers its liveness half: no automatic leader election, no failure detector, and split-brain reconciliation beyond last-epoch-wins is undesigned | Opt-in epoch-claim election folded from the eventually-consistent membership index with a stated convergence/liveness bound and a generative leader-churn harness; a failure-detection window that does not become a second heartbeat protocol; an application-level reconciliation hook for fenced divergent writes; an optional ack-from-k durability tier; and per-shard leader routing when partitions replicate (93 I-25/I-2/I-3/I-8) | 40/42, 20/23, 20/24 |
| G-45 | *(W3.3, narrowed)* The gossip-mesh skeleton still lacks its liveness/churn **argument**: membership-churn reconvergence is unproven and graceful last-replica handoff to durable storage is undesigned. **Built**: the eviction gate itself (membership-gated suspend-when-partitioned + drain-gated despawn, `Replication.evict`), peer-side reconciliation on despawn (`RegistryAnnounce.unpublished`), and the replica-convergence invariant harness with its departed-stream rule (`cell.verify.ReplicaConvergence`) — a departed replica no longer false-positives a live divergence | State the bounded-gossip-hop reconciliation argument (duplicate/stale mesh links safe by tag idempotence) with a generative membership-churn harness (R1, 95); define graceful last-replica handoff to durable storage (G-25) vs accidental deletion | 40/42, 50/52 |
| G-54 | *(W4.1, core landed; amended T02/remediation)* `civictech.cell.membrane.BoundaryPolicy` (four predicates: linkAuthority, protocolAuthority, disclosure, integrity) bound to a `CompositeCell` `Exposure`, evaluated at the three seams. **Residual**: the original design carried a fifth predicate, `admission` (a `PeerPredicate`); it had zero read sites (`forcesMediate` explicitly excluded it) and was deleted (remediation T03) — ingress admission is enforced solely by `Peering.Allowlist`/`allowPeers(...)` (G-29 phase 1), never by `BoundaryPolicy` — `mediate()`'s `linkAuthority` (seam 2), `mediateOutlet()`'s `disclosureFilter`/`ProtocolSupport.inboundFilter` (seam 3 outbound: attention ceiling clamp + disclosure, filtering catch-up and live uniformly), and `MediateProxy`'s `RequireSigned` verify-at-ingress with a per-source replay counter (seam 3 inbound) — `AuthLevel.TransportVouched` only. **Residual**: capability hand-out/revocation for exposed ports and taps (tearing down live links, not just refusing new ones), management-plane authority for remote graph mutation across a bridge, multi-hop/nested-membrane composition of disclosure and integrity, and encryption at rest | Exposed refs and taps as revocable capabilities where revocation tears down live links rather than only refusing new ones (tap attachment governed by the same promotion/link authority membrane); an authority model for who may drive PORT_MANAGEMENT (spawn/connect/despawn/swap) and attach observers across a bridge; hop-composition rules for disclosure/integrity when a peer relays disclosed state; and an at-rest encryption stance for durable journals and parked/overflow state (93 I-28 §8) | 40/43, 10/13, 20/24 |
| G-66 | T05 finding 8 (filed, not fixed): a *legitimate* `Interest.sliceTo` result — a `Scoped` delta that genuinely slices to nothing for one peer (`replication/Replication.kt:510`, `partition/PartitionedShardSet.kt:171`) — mints no `Progress` absorb-ack on that specific edge. `control/AbsorbAck.kt:7-23`'s `FanOutlet.absorbAck()` fans a `Progress` over the outlet's *entire* real-link set; it has no per-link form, so it cannot single out just the one edge whose slice emptied. A downstream per-source `WaveFrontier` watermark on that edge can under-advance | A per-link ack variant — mint `Progress` on the ONE edge whose slice was empty, not the whole outlet's link set. M-effort; interacts with T07's replication/partition territory (the two call sites live there) | 40/42, 20/22 |
| G-67 | `42-replication.md`'s leader/follower split specified an opt-in `WritePosture.SAFETY_PARK` (a leader that cannot confirm it is un-superseded parks writes instead of serving divergent ones); `WritePosture` and both its values (`AVAILABLE_FENCED`/`SAFETY_PARK`) had zero production installs and were deleted (remediation T03) — every leader today runs the always-fenced behavior unconditionally, with no per-cell posture switch | Not a redesign — the always-fenced default stays exactly as landed. An opt-in `SAFETY_PARK` posture (and the follower-unpark rule it would need) can be reintroduced with its first real user once a concrete case needs park-on-uncertainty over the fenced-and-reconcile default | 40/42 |

## Gaps — process (50)

| ID | Gap | Proposal | Where |
|---|---|---|---|
| G-30 | No graph DSL / declarative construction | **Resolved (M4.5)**: `cell.graph` builder lowering to `spawn`/`connect` management invocations; records a serializable `GraphSpec` (graphs-as-data) that replays onto any host — serialization round-trip verified. Identity-aware, hierarchical, and remote application design-decided (93 I-21), not built (remote-apply failure semantics → G-51) | 50/51 |
| G-31 | No invariant machinery | **Resolved (M4.4 + M4.6)**: `InvariantCell` (fold + check, violations outlet; attaching = linking) + `checkInvariants` kotest adapter + generative graph harness (seeded random `GraphSpec` pipelines with late-join/migration events, invariant suite, order-bias control) on the deterministic `SimulationController` | 50/52 |
| G-32 | No shadow mode (side-effect suppression) | **Resolved (M9.2)**: `Effectful` marker + `Shadow.spawn` NoOp-serves effectful inlets; judged by invariant cells; double-fire control verified. Amended by decision (93 I-17): suppression cuts at `@Contract(effect=true)` boundary contracts, the cell-level marker demoted to a coarse fallback that terminates judgeability downstream — **landed** (computenet-3jv2): `Shadow.spawn` NoOp-serves every `FanInlet` whose `ContractDescriptor.effect == true`, in addition to the `Effectful`-cell fallback. Still unbuilt: the rest of the 93 I-17 observation membrane (downstream-only taps, SCC-closed cuts, gate-invariant consistency points); exclusive flows observed via Borrowed taps (93 I-20 → G-47; discharging-sink divergence = C-11) | 50/52 |
| G-33 | No state migration across instances | **Resolved (M9.4)**: `StateMigrating.importFrom(snapshot)` in `Promotion.promote`'s buffered window; non-migrating cells fall back to catch-up replay. Composed contract decided (93 I-27, reconciled with I-22/I-14): the catch-up fallback tier (T2) is restricted — unsound for non-idempotent-across-source cells (e.g. `CounterCell` → T0/T1) — and MUST emit the ReBaseline supersession notice; residuals → G-49 | 50/53 |
| G-1 | Legacy packages coexist with germ (two kernels in-tree) | **Done (M3.1)**: the `:legacy` module is deleted. The kernel is `civictech.cell`. The `gen.async` remnant (`GenerateSuspended`, superseded per 51 §Code generation) is retired too | 00/03, 30/32 |
| G-2 | `ManagedRunner` duplication | **Resolved** with C-2 (class no longer existed; scheduler configuration covers the use case) | 30/31 |
| G-49 | The two-phase swap + state-transform design is by-convention at its load-bearing spots | **Resolved for the swap transaction (W3.5)**: `Promotion.promote` is an explicit PRECHECK → PREPARE → COMMIT → RETIRE state machine with a retained incumbent and non-vetoing commit; PRECHECK refuses promotion for a `NonIdempotentCatchUp` candidate lacking a T0/T1 transfer; COMMIT runs T0/T1 (`importFrom` + `adoptWaveState`) or the T2 fallback (`mintFreshEpoch` + `ReBaselineEmitting.reBaseline`); a mid-COMMIT failure (`PromotionAborted`) rolls back every completed relink and re-greens onto the incumbent; RETIRE (`despawn`) runs only after a successful COMMIT. Still by-convention: a contract-version discipline guarding `importFrom` `schemaVersion` against same-FQN hash collisions; an explicit non-promotable declaration for hidden-state cells; a retention window for the retired incumbent's export snapshot with rollback-*after*-retire journal-reversal semantics; and a transform-correctness generative harness (93 I-11/I-27/I-21). Coupled-flow windows remain G-53 | 50/53, 20/24, 10/13 |
| G-50 | **Policy half resolved (W4.4)**: `civictech.cell.evolve.PromotionPolicy` (`gates`, `ObservationWindow` in observed waves, `SatisfactionCriterion` with a strict zero-violations default, `judge`, optional differential `baseline`) is evaluated by `PromotionJudge`, consulted by `Promotion.promote`'s PRECHECK; the differential shadow enforces no-worse-than-incumbent when `baseline = true`; cycle promotion gates on quiescence via a judge-supplied `cycleHead`, deferring (never accepting) unless `FeedbackInlet.lastQuiescent == true`. Residual: who may register or trigger a swap is still ungated (no authority check runs before `Promotion.promote`), and canary staging plus multi-partition rollout orchestration are unbuilt | Registration/trigger authority gated by the membrane/policy layer under federated governance; a small-blast-radius canary staged-promotion path for unshadowable closed-loop candidates; and an ordering/monitoring/abort policy for partitioned rolling promotion (93 I-21/I-17/I-27) — M15.5 | 50/53, 40/43 |
| G-51 | GraphSpec application to remote hosts has undefined failure semantics: partial apply leaves an unknown graph, per-step reporting has no contract, and structural validation timing (build vs replay) is unstated | **Resolved for the core (W3.6)**: `SpawnStep` carries `IdentityBinding` (`FreshLogical`/`NewInstanceOf`/`Exact`) + an optional `parent`, and a ref-aware `CellFactory` — the wire-crossing construction form, never a live `Cell`. `HostManagementApi.spawnBound(factory, identity, parent)` resolves the binding host-side and admits through the same guard `spawn` uses, so re-applying an `Exact` spawn of a live ref hits the ordinary live-ref spawn guard and dead-letters (idempotent re-apply). `GraphSpec.applyRemote` degrades loud failure from synchronous (`applyTo`, unchanged) to asynchronous: rejected steps dead-letter on the target host and fold into a returned `ApplyReport` instead of aborting or throwing to the applier — **partial-apply, always leave-and-report**. Still open, research-gated (95 §R4): compensating rollback of the successful prefix (partial-apply *atomicity*), a dataflow-linkable `ApplyReport` outlet (today's report is a direct return value, not a subscribable port), and eager cold structural pre-validation | 50/51, 10/15 |

> G-34 and above originate from the feature-interaction analysis ([93](93-feature-interactions.md), challenges I-1..I-28); each row cites its source challenges in the Proposal cell. C-9..C-12 record where landed code diverges from those decided rules — except C-12, which on adjudication (D-C12) turned out to record where the *prose* had gone stale rather than where the code diverged.

## Reading the dependency structure

The gaps are not flat; the critical path is narrow:

```
G-4 (context)  ──►  22 glitch-freedom, tracing, G-23 causal merges
G-12 (links/handshake) ──► G-14 policies ──► membranes G-10, security 43
        │                        └─► G-21 ownership enforcement
        └─► unlink ──► G-5 mobility ──► G-15 wire ──► G-7 replication ──► 53 evolution
G-17 (port registry) ──► C-5 de-reflection ──► G-15 KSP wire layer
```

Everything else (data cells, colors, DSL, invariants) hangs off these four
enablers: **context, links, port registry, drain protocol**. See 92 for the
proposed sequencing.
