# Line budgets — a RATCHET, not a cliff.
#
# WHY. Adding text to a skill is currently free, and the log shows what free
# costs: work/SKILL.md went 668 -> 2236 lines in the twelve days to 2026-08-20,
# 3.3x, plus 21 reference files. The rubric gate has computed that number and
# warned `ideal <=500` on every run for weeks, and every session — including
# the seven-commit drain of 2026-08-20 — read the warning and proceeded. A
# warning nobody acts on is not a check.
#
# Nothing here demands a restructure. Each budget is set at the size the skill
# ALREADY IS, so today's tree passes untouched. What changes is the price of
# growth: a session that adds 40 lines must remove 40, or raise the number
# below — one line, in the diff, where a reviewer and a human can see it and
# ask what it bought. That is the question no mechanism currently asks.
#
# Lower a number whenever a drain removes text. That is the ratchet: the budget
# can fall silently and can only rise deliberately.
#
# Format: <skill-dir-name> <max-lines>
# Counted as SKILL.md's body (frontmatter excluded) plus every references/*.md,
# because the context cost of a skill is everything an agent is sent to read.
#
# Enforced by scripts/validate-skills.rb, which FAILS above the budget.

# 8055 -> 8059 on 2026-08-20: step 4's twin scan became a command
# (scripts/twin-scan.py) instead of "eyeball the --all listing for same-titled
# children". Net +4 lines of SKILL.md for a check that finds all six known
# computenet-4ru twins where bv --robot-suggest finds none. Paid rather than
# squeezed: the alternative was deleting someone else's incident-backed rule to
# make room, which is the wrong incentive for a budget to create.
# 8059 -> 8069 on 2026-08-21: computenet-ex6w — a mutation must not overlap the
# original (prefix-preserving renames, coerced numbers, glob-matching paths).
# 8069 -> 8075 on 2026-08-21: computenet-vjrs (see PR body).
# 8075 -> 8082 on 2026-08-21: computenet-xbkm — route 0 contention stated in terms of the repo-wide gate.
# 8082 -> 8090 on 2026-08-21: computenet-d52d — rebased onto main (see PR #425).
# 8090 -> 8095 on 2026-08-21: computenet-5b34 — rebased onto main (see PR #423).
# 8095 -> 8106 on 2026-08-21: computenet-jobe — rebased onto main (see PR #422).
# 8106 -> 8114 on 2026-08-21: computenet-l8ju — rebased onto main (see PR #416).
# 8114 -> 8122 on 2026-08-21: computenet-9cji — rebased onto main (see PR #439).
# 8122 -> 8128 on 2026-08-21: computenet-af9q — rebased onto main (see PR #432).
# 8128 -> 8136 on 2026-08-21: computenet-af9q — rebased onto main (see PR #432).
# 8136 -> 8142 on 2026-08-21: computenet-9r2z — rebased onto main (see PR #436).
# 8142 -> 8145 on 2026-08-21: computenet-7xeh — rebased onto main (see PR #443).
# 8145 -> 8154 on 2026-08-21: computenet-tjyl — rebased onto main (see PR #438).
# 8154 -> 8161 on 2026-08-21: computenet-tjyl — rebased onto main (see PR #438).
# 8161 -> 8167 on 2026-08-21: computenet-bz5c — rebased onto main (see PR #421).
# 8167 -> 8171 on 2026-08-21: computenet-kklt — rebased onto main (see PR #420).
# 8171 -> 8176 on 2026-08-21: computenet-6664 — rebased onto main (see PR #431).
# 8176 -> 8191 on 2026-08-21: computenet-ozgs — rebased onto main (see PR #410).
# 8191 -> 8192 on 2026-08-21: computenet-ozgs — rebased onto main (see PR #410).
# 8192 -> 8199 on 2026-08-21: computenet-6ag4 — rebased onto main (see PR #424).
# 8199 -> 8205 on 2026-08-21: computenet-6ag4 — rebased onto main (see PR #424).
# 8205 -> 8214 on 2026-08-21: computenet-zy98 — rebased onto main (see PR #441).
# 8214 -> 8225 on 2026-08-21: computenet-lq0p — rebased onto main (see PR #417).
# 8225 -> 8231 on 2026-08-21: computenet-wv9c — rebased onto main (see PR #433).
# 8231 -> 8242 on 2026-08-21: computenet-wv9c — rebased onto main (see PR #433).
# 8242 -> 8248 on 2026-08-21: computenet-wv9c — rebased onto main (see PR #433).
# 8248 -> 8255 on 2026-08-21: computenet-w8cp — rebased onto main (see PR #434).
# 8255 -> 8264 on 2026-08-21: computenet-w8cp — rebased onto main (see PR #434).
# 8264 -> 8273 on 2026-08-21: computenet-mzuc — rebased onto main (see PR #428).
# 8273 -> 8276 on 2026-08-21: computenet-wv64 — rebased onto main (see PR #435).
# 8276 -> 8286 on 2026-08-21: computenet-db3k — rebased onto main (see PR #442).
# 8286 -> 8291 on 2026-08-21: computenet-db3k — rebased onto main (see PR #442).
# 8291 -> 8295 on 2026-08-21: computenet-jd4q — rebased onto main (see PR #440).
# 8295 -> 8306 on 2026-08-21: computenet-wahz — rebased onto main (see PR #413).
# 8306 -> 8310 on 2026-08-21: computenet-4dip — rebased onto main (see PR #437).
# 8310 -> 8326 on 2026-08-21: computenet-f7gy — rebased onto main (see PR #430).
# 8326 -> 8341 on 2026-08-21: computenet-wliv — rebased onto main (see PR #429).
# 8341 -> 8342 on 2026-08-21: computenet-wliv — rebased onto main (see PR #429).
# 8342 -> 8352 on 2026-08-21: computenet-nb44 — rebased onto main (see PR #426).
# 8352 -> 8355 on 2026-08-21: computenet-nb44 — rebased onto main (see PR #426).
# 8355 -> 8368 on 2026-08-22: computenet-dy7q — nested-KDoc glob entry; quoted --include inline.
# 8368 -> 8392 on 2026-08-21: computenet-hc3s — rebased onto main (see PR #427).
# 8392 -> 8403 on 2026-08-21: computenet-hc3s — rebased onto main (see PR #427).
# 8403 -> 8409 on 2026-08-21: computenet-hc3s — rebased onto main (see PR #427).
# 8409 -> 8414 on 2026-08-21: computenet-hl8x — rebased onto main (see PR #415).
# 8414 -> 8577 on 2026-08-24: 17-item SDLC drain in one PR (this branch): 163
# lines bought 23 friction fixes across SKILL.md + 10 references — among them
# nkz3's STALE holder verdict, qmjd's gate-scoping rule, v5ah's fourth-recurrence
# rewrite of the wait instruction, 9swr's cargo proof standard, and bgdb's
# read-tool notes. Six of the 23 are mechanical (scripts), priced separately
# at zero here.
# 8577 -> 8590 on 2026-08-29: computenet-hil5 — wait-checks.sh's window is
# smaller than build-test-fast and cannot be widened inside the 600000 ms
# foreground cap, so two invocations is the ordinary cold start. 13 lines state
# that contract at both call sites (SKILL.md step 2, review-feature.md §4);
# the script-side ORDINARY/STUCK annotation is mechanical and free here.
# 8590 -> 8630 on 2026-08-29: three more in the same drain. computenet-zwju
# (+18 in bd-traps.md) — bd show inlines the parent body once PER DEPENDENCY,
# measured 149KB from one call; the redirect rule it recurred past is now
# backed by scripts/bead.sh, a projection (57KB -> 7KB) that needs no
# per-dispatch warning. computenet-g1gf (+7) — --desc-file/--accept-file are
# the WRAPPERS' flags; bare bd create takes --body-file and has no acceptance
# file flag, so step 7 now names the safe form for both paths.
# computenet-jm7k (+15) — a files claim COPIED from a sibling is
# orchestrator-authored and gets af9q's invariant grep; a sibling's claim is a
# lower bound, never the answer.
# 8630 -> 8662 on 2026-08-29: computenet-hs90 (+12, step 2) — never WRITE an
# elapsed figure not computed that turn; a session drifted ~100m low for two
# hours by extrapolating. computenet-kp0y (+20, review-feature.md §4 and
# agent-execution.md) — sixth recurrence of the stalled-turn defect, all six
# of them a reviewer waiting on gh pr checks: reviewers now state a verdict
# conditional on pending checks and STOP, and the missing FACT ("you have no
# inbound wake-up") is stated ahead of the prohibition. computenet-00d8's
# sha-binding of wait-checks.sh is mechanical and free here.
# 8662 -> 8713 on 2026-08-29: computenet-ahg8 (+14) — the $BD-in-a-variable zsh
# trap recurred past computenet-jobe, so the function form now sits where -C is
# PRESCRIBED (bd-traps.md), not only where the trap is catalogued; and the
# explicit-timeout rule is stated as a host rule, not a Gradle rule (a bare echo
# was killed at the 120s default). computenet-ltv9 (+18) — the budget monitor
# has now gone silent twice by two routes, so the hand subtraction is named THE
# mechanism and each tier reports the elapsed it computes rather than the sleeps
# it slept. computenet-geky (+19) — ask where the PINNING TEST will live, and
# apply the rule to any claim not derived from the code, residual-filed included.
# 8713 -> 8730 on 2026-08-29: computenet-yb4s (+5) — bd list --parent DOES
# return descendants (measured, bd 1.1.2); bd ready --parent is the one-level
# subcommand, and the SDLC-exclusion line named the wrong one. computenet-qvrd
# (+8) — --set-metadata k= stores "" rather than removing the key, so
# ship-feature.md §4 tests the VALUE. computenet-s16r (+4) — junit-count.py is
# executable with a shebang, so the bare-path citation runs; say so once.
# computenet-yiof is a one-word substitution and free.
# 8730 -> 8764 on 2026-08-29: computenet-7vsj (+11) — review-feature.md gave
# two opposite parents for a reviewer-filed bead; the distinction (does the
# feature WAIT for it?) is now named, because an epic-parented blocker blocks
# nothing. computenet-89jr (+12) — git stash is one repo-wide stack shared by a
# dozen linked worktrees, and nothing in the references said so.
# computenet-9z8t (+10) — bd update aborts the whole call on one unknown flag,
# discarding a valid flag's write; --body-file exists, --acceptance-file does
# not. computenet-l1bb is one inline clause on an existing table row.
# 8764 -> 8813 on 2026-08-29: computenet-dowo (+19, bd-traps.md) — bd writes a
# pagination TRAILER after the closing bracket when a listing is capped, and the
# documented slice ran to end-of-file; the idiom is now terminated
# (/^[[{]/,/^[]}]/p) at all 34 sites and the "empty jq field is not an unset
# field" rule is stated. computenet-i4cq + computenet-1z8s (+19, gradle-evidence
# .md) — six agents across two sessions: the same task twice on one command line
# runs once under the FIRST --tests filter, and the actionable-task line points
# the wrong way. computenet-1mjv (+11) — --rerun on a LIFECYCLE task reruns
# nothing while looking like correct usage.
# 8813 -> 8849 on 2026-08-29: computenet-2jyq (+11) — SETTLED means no row is
# PENDING; a FAILED required check settles identically and the next move is
# `gh pr ready`, which merges itself. Stated in step 2, with the script naming
# red rows above the verdict as a backstop. computenet-umw4 (+16) —
# create-ticket.sh can be DENIED inside a dispatched subagent, and the obvious
# workaround is the forbidden destructive one; the manual equivalent and the
# separate-call rule for body files are now written down. computenet-k5a9 (+9)
# — the JVM-provenance grep matched JMH's Unsafe warning instead of the banner.
# 8849 -> 8869 on 2026-08-29: reviewer findings on this same drain. +6 in
# SKILL.md — the self-checking monitor tiers introduced a way to silence the
# monitor COMPLETELY (an unreadable slot-start makes the arithmetic expansion a
# fatal error, killing the shell at tier one), so the block carries a literal
# path and a `|| echo 0` fallback and says why. +14 in gradle-evidence.md and
# review-task.md — the two faults that both read as "fewer JUnit files than
# expected" (computenet-5b34 deletion vs computenet-i4cq duplicate task) now
# point at each other, and computenet-l1bb's sibling prose site gets the
# command. The corrections to bd-traps.md's stderr claim, SKILL.md's stale
# script-table row and bead.sh's header are net zero.
# 8869 -> 8938 on 2026-08-29: computenet-g8ho (+69) — recurrence of
# computenet-pi3h. The skill made the mutation check non-optional for the
# implementer while its own file-claim rule forbade the file the mutation needs,
# and the permission classifier enforced the forbidding: measured twice in one
# session, an identical edit refused to the implementer and allowed to the
# reviewer whose dispatch named the authorization, plus a read-only `gh run
# list` refused to a subagent and allowed to the orchestrator. Production
# mutations outside the claim are now the REVIEWER's step by design; the
# authorization sentence moves out of ad-hoc prompt wording into both dispatch
# templates; and the perl/sed detour is scoped to tool refusals, never to scope
# limits. Without the reviewer's retry that session would have merged a 461-line
# convergence test whose central property was never shown to fail.
# 8938 -> 8973 on 2026-08-29: computenet-0frx (+35) — the "N actionable tasks"
# line misled two agents in OPPOSITE directions in one session. False green:
# the plain unfiltered `./gradlew :kernel:test` came back UP-TO-DATE with zero
# tests run, while AGENTS.md framed the trap around a --tests-FILTERED
# invocation, so the agent read itself as outside the warning. False alarm:
# `:concord:test --rerun` prints `1 executed, 23 up-to-date`, which reads like
# a cache replay against a 24-task graph, and three more agents each spent a
# cross-check on it. All four were settled by the JUnit `newest` timestamp
# junit-count.py already prints. The lines demote the task count to a
# corroborating signal, name `newest` as the verdict, and put --rerun in the
# module-gate command task.md step 6 prescribes rather than only in prose
# elsewhere. AGENTS.md's own paragraph is generalised in the same commit.
# 8973 -> 9020 on 2026-08-29: computenet-y6zv (recurrence of computenet-m9px)
# + computenet-os91, one shape. af9q's rule finds the files that observe the
# INVARIANT a change moves; these close over the SET — a registry's
# completeness, a package's census — so they never name the entry being added
# and no grep for it reaches them. Measured: PR #544 added UntagCell.kt to
# civictech.cell.data.op with a two-file claim, passed check-files-claim.sh
# CLEAN, was certified by mutation, ran :kernel:test green at 1273 tests, and
# build-test-fast went red in :inspect and :oracle. 46 lines buy the enumerator
# walk, the two shapes named with their repo instances, and the explicit
# statement that neither check-files-claim.sh's CLEAN nor a green module gate
# can falsify this class. The four new COUPLINGS rows are mechanical and free
# here — they fire on computenet-j2x.3.1, naming all three enumerators that
# reddened #544, where the current script prints nothing.
# (of which +1 on the review round:) the same bead's review round (PR #559). +1 naming
# OperatorInventoryTest and its inventory file in the completeness-gate list, so
# all three of computenet-os91's instances are in the prose rather than two of
# them being one indirection away in the script's table. The add-only narrowing
# of the couplings is mechanical and free here.
# 9020 -> 9051 on 2026-08-29: computenet-uxr8 (+31, one of them the blank line
# that keeps the gradle-evidence.md sentence its own paragraph) — "affected module tests"
# was read as the module the diff SITS IN, unanimously, by five agents in a row.
# Feature computenet-j2x.4 added four files to concord/corpus/24-data-cells/;
# :oracle's CorpusCrossCheckTest enumerates that directory at test time and
# asserts a non-vacuity count, so build-test-fast went red on
# `expected:<29> but was:<33>` after four task merges. No import, no Gradle
# dependency, no path overlap: nothing static can surface a runtime directory
# read. The lines redefine "affected" as the modules whose test INPUTS the diff
# changes, at both call sites (review-feature.md §3, task.md step 6), with the
# concord/corpus -> :oracle:test rule stated concretely so it needs no derivation.
# 9051 -> 9096 on 2026-08-29: computenet-ci1g (+47) — direct-child.md's three
# decided mechanics all assume a diff, and one of them is not skippable: no
# commit means no PR, and the PR is a PRECONDITION of dispatching the reviewer
# (computenet-a4cj). So a tracker-only item — deliverable is BEAD TEXT, a shape
# review-feature.md §7 residuals produce routinely, because a reviewer may write
# only to the item under review — had NO route, and a session with two of them
# invented one under time pressure and landed in the seat SKILL.md warns about.
# The lines give the recognition rule (empty-or-beads-only metadata.files AND a
# bead-text deliverable, distinguished at review from BOTH `files unknowable
# before diagnosis` and the many ordinary code beads filed with the field
# absent), declare the three mechanics not applicable, and settle the review
# question: dispatch the edit, and the orchestrator's read-back is sufficient
# BECAUSE it is not the author — with a second dispatch required if it is.
# +8 of the 47 answer the objection the reviewer raised and the first cut left
# a future reader to reconstruct: this route DOES collapse 5e's certify/ship
# split, and the reason that is safe here is mechanism-specific (5e's hazard is
# a ready PR merging itself into main; there is no PR, no auto-merge and no CI
# on this route, and a bead field reverts in one command).
# 9096 -> 9130 on 2026-08-29: computenet-pko4 (+34). A test with several
# assertions can be reddened by an EARLIER one while the assertion carrying the
# criterion never discriminates: LinkControl.holding's first test PASSED with
# the primitive no-op'd, and a sibling case left five tests green with the
# duplicator neutralised entirely. Both were caught by agents who happened to be
# thorough; the documented procedure passed both. +21 in mutation-check.md step
# 4 and "What to report"; the other +13 restate the clause at the three call
# sites (task.md step 3, review-task.md, review-feature.md) where the verdict is
# actually recorded.
#
# computenet-a4b7 rode this branch and is being CLOSED NOT-REPRODUCIBLE instead.
# Its diagnosis — that re-applying an identical mutation leaves compileKotlin
# UP-TO-DATE with a stale class, uncovered by --no-build-cache — was measured on
# :nature by two reviewers and is false in every part: a content change always
# re-executes, the CACHE is the content-keyed half, and a FROM-CACHE restore
# gives the class that is CORRECT for the source on disk (proven by javap). Its
# proposed remedies fail too: `touch` recompiles nothing, and `find -newer`
# returns empty in a healthy tree as well as a stale one, so it separates
# nothing. 5 lines survive here — the part that measured true and tells an agent
# something to do. The other ~30 were a refutation with no action in it, which
# is a bead comment, not a procedure file.
# 9130 -> 9150 on 2026-08-30: computenet-2r22 (+20) — 16 lines bought 5b's
# "scoping the gate is not sufficient" paragraph and the wall-clock-await rule
# for going under the cap; the other 4 are the review's: the clause recording
# that the cap's own arms were scoped, and the half-sentence keeping "scope
# anyway" from being read as license to skip the paragraph above it. Recurrence of computenet-qmjd, whose prose remedy was followed
# exactly and still reached load 204 on 16 cores. The mechanical half —
# capacity.load1/advice from next-batch.py's load_advice() — costs no budget
# lines, being script, and carries its own derivation in its docstring.
# 9150 -> 9160 on 2026-08-30: computenet-1lbs (+10) — bought the hookup for
# slot-elapsed.sh, which prints the AGE of the previous elapsed reading (the
# script itself is not counted here). Third report of this friction
# (computenet-776 -> hs90 -> 1lbs); the first two fixes were prose that made
# each reading true and said nothing about how fast a true reading goes stale.
# ~24 lines of prose rationale were compressed out of step 2 to hold the growth
# down; 3 of the 10 are the review's must-fixes (the "largest in busy sessions"
# clause the acceptance requires, and a sample output that had been wrong).
# Recomputed against 2r22's LANDED 9150 rather than merged with the 9140 this
# branch carried — two PRs raising the same number always conflict and the
# second depends on the first's landed value (computenet-x69c).
# 9160 -> 9166 on 2026-08-30: computenet-dh5x (+6) — bought gradle-evidence.md's
# statement that a path junit-count.py could not RESOLVE OR READ answers
# NO-SUCH-PATH (exit 2), not NO-RESULTS, and that neither code is evidence
# about a suite; +2 of the 6 correct an adjacent pre-existing sentence that
# told callers a module root exits NO-RESULTS, which the v38r patterns made
# false. The mechanical half is in junit-count.py and costs no budget lines.
# RECOMPUTED against 1lbs's landed 9160, not merged with the 9156 this branch
# carried — the second number depends on the first's landed value
# (computenet-x69c, whose fix for this is PR #594).
# 9166 -> 9167 on 2026-08-30: computenet-x69c (+1) — direct-child.md's
# "~4 min" per-merge check cost predated the current six-check set and is now
# ~9-12m wall (measured 2026-08-30). Corrected in the same diff as the lane
# text that cites it, so the cite and the citee cannot drift apart.
# 9167 -> 9173 on 2026-08-30: computenet-a5in (+6) — bought 5e naming the
# fourth check state (NO-RUN, exit 5, never waited out, because GitHub started
# no run for this head), plus the review's: the same state in
# review-feature.md's enumeration, which the bead named and the first pass
# skipped, and the correction that there is no workflow to re-run — ci.yml has
# no workflow_dispatch, so a push is the only remedy.
# The mechanical half is in wait-checks.sh and costs no budget lines.
# 9173 -> 9180 on 2026-08-30: computenet-f38z (+7) — bought review-feature.md
# saying that lint-workflow.rb now walks composite actions too, and that a
# NOTHING CHECKED line is never coverage. It walked only jobs.*.steps[], so on
# .github/actions/** it printed "0 run-blocks checked" and exited 0 — a vacuous
# lint cited as coverage over a CI safety net that had just been factored INTO
# a composite action, i.e. the assertion itself was the unlinted script.
# The mechanical half is in lint-workflow.rb and costs no budget lines.
# 9180 -> 9190 on 2026-09-01: computenet-6eyp (+10) — bought the zsh `=word`
# trap in epic.md and feature.md, the two files a BREAKDOWN dispatch reads.
# The warning already existed, in agent-execution.md, which breakdowns are not
# given: six hit it across two sessions, four after computenet-a49j closed.
# Duplicated rather than LINKED, twice over: the defect being fixed is a
# one-hop indirection nobody followed, and a breakdown reads its reference as
# one `git show origin/main:<path>` blob, so a relative link is not a click.
# It also names agent-execution.md in prose rather than as a markdown link, so
# reachability.py still reports that file NOT-READ for the breakdown role —
# a link would have flipped the gate to SERVED and spent the signal that
# catches the NEXT misplacement there.
# 9190 -> 9192 on 2026-09-01: computenet-3qdo (+2) — bought step 2's
# description of wait-checks.sh saying the required set is READ FROM THE
# RULESET each run. The literal 'all six' was the same drift the script
# carried: the ruleset went to seven on 2026-08-31 and a PR shipped BLOCKED.
# The mechanical half is in wait-checks.sh and costs no budget lines.
# 9192 -> 9195 on 2026-09-01: computenet-i5zr (+3) — bought 5b's documented
# return shape carrying `warnings`, and what a DIRECTORY claim costs. The
# detector is mechanical and free; these 3 lines are what makes the orchestrator
# act on it. Recomputed against the landed 9192, not stacked on the number
# this branch was cut from — two PRs raising one entry always conflict, and
# the second number depends on the first's landed value.
# 9195 -> 9203 on 2026-09-02: computenet-v8kg (+8) — bought the statement that
# the monitor's three tiers ARE the slot end minus 105m/60m/15m and are absolute
# offsets, not fractions of the slot, plus the replacement tie-breaker (take the
# more cautious rung and file it). The old text said "scale proportionally" and
# handed a 15-minute window at every rung to whichever source permitted more
# work; the mechanical half — slot-elapsed.sh's Finalize reserve and a 13-row
# boundary grid — costs no budget lines. The 8th line is the monitor block's
# own `SLOT=18000`, which lets the first tier's sleep be DERIVED ($((SLOT-6300)))
# instead of hand-computed per slot length: one fewer place to get wrong.
# 9203 -> 9209 on 2026-09-02: computenet-tl8q (+6) — bought the correction of
# two sentences this fix falsified. Both said the wait window is "~9m20s, sized
# to the 600000 ms cap"; sizing it AT the cap is exactly what got the call
# auto-backgrounded twice, and the script now bounds itself on wall clock at
# ~8m10s. Leaving them would re-open, in the other direction, the script-vs-prose
# divergence this bead is about. The mechanical half costs no budget lines.
# 9209 -> 9219 on 2026-09-02: computenet-z6q2 (+10) — bought 5b's return shape
# carrying `running_elsewhere`, and the carve-out on the verify-ready paragraph
# saying a READY task whose skip reason names a live unit outside the feature
# must NOT be dispatched by hand. The mechanical half (next-batch.py seeding
# its disjointness test with those claims) costs no budget lines, but it is
# invisible: the orchestrator reads the documented return shape, and the
# verify-ready line routed straight back into the bug. Recomputed against the
# landed 9209, not stacked on the number this branch was cut from.
# 9219 -> 9235 on 2026-09-02: computenet-98cu (+16) — bought review-feature.md's
# opening banner: the file EXCEEDS ONE READ CALL (the cap lands near line 1016 of
# 1200), and the two rules that decide a reviewer's output — the literal
# READY/DRAFT verdict token and the residual-filing protocol, both in §§7-8, past
# the cut — hoisted so a one-page reader has them. Three reviewers reached them
# only by paging on unprompted, and 5e refuses to act without the token. Prose in
# the file is the only fix available for the file that has the problem; the
# mechanical half — a validator rule failing any reference over 900 lines that
# does not say so in its first 50 — costs no budget lines and is what makes the
# NEXT file to cross the line say it too.
# and +3 more for the reviewer-dispatch template's "PAGE TO THE END" clause,
# which is the bead's second acceptance criterion: the banner only helps a
# reader who opens the file, and the template is what a dispatched reviewer
# reads first.
# 9235 -> 9245 on 2026-09-02: computenet-9src (+10) — bought 5b's statement that
# the text check and the invariant symbol grep are COMPLEMENTARY and in which
# order, plus the exception to "blind to anything that is not a path". The root
# cause was not a weak script: on computenet-t446 the orchestrator ran the newer,
# more elaborate symbol grep INSTEAD of this one, and a better script cannot fix
# not being run. Recomputed against the landed 9235.
# 9245 -> 9257 on 2026-09-02: computenet-s7az (+12) — bought the git grep \s
# entry in agent-execution.md's "Commands that fail QUIETLY" list. The trap was
# first written into AGENTS.md, which reachability.py says the ORCHESTRATOR
# reads and the four dispatched roles do not — and the reporter was a feature
# REVIEWER, hitting it while diffing symbols across revisions, which only git
# grep can do. That is the computenet-l5rc shape exactly, and l5rc's own remedy
# is dual-placed for the same reason. Recomputed against the landed 9245.
# 9257 -> 9273 on 2026-09-03: computenet-9p3y + computenet-s6x5, one edit for
# both. review-feature.md §4 and review-task.md §3 stop enumerating the
# required checks (a count wrong in BOTH directions across four instances) and
# point at the ruleset via wait-checks.sh; §4 gains the non-required-lane
# reading that would have stopped a false "CI covers none of :iroh" reaching
# three dispatch prompts and a PR body. +16 lines to delete two lists that go
# stale on every promotion. 9273 -> 9275 in review: the reviewer found two
# stale counts the sweep had missed inside review-feature.md itself ("six green
# rows", "the other five settle") and a worked example that still called
# iroh-sidecar non-required — the very inversion the bead is about.
# 9275 -> 9285, RECOMPUTED on rebase against the landed number rather than
# merged with the 9257 baseline it was written from (2026-09-03): computenet-axxl, two of its three one-line
# documentation gaps. issue-quality.md gains the "next free X goes stale"
# bullet (a bead named F-14; another PR took it first), review-feature.md's
# `bd comments --json` recipe names `.text` (the wrong key returns null, which
# reads as an empty thread). The third gap — create-ticket.sh's --help — is a
# script and costs the ledger nothing.
# 9275 -> 9335 on 2026-09-03, RECOMPUTED on rebase (measured against the
# 9275 -> 9339 on 2026-09-03, RECOMPUTED on rebase (measured against the
work                RECOMPUTE
# landed 9275, not merged with the 9257 baseline it was written from): three
# items in one PR, all in SKILL.md, so they
# raise this entry once instead of colliding on it three times
# (computenet-x69c's lesson).
#   computenet-lx7t (+24): agent COUNT is not the load model — ONE repo-wide
#     gradle gate alone took 16 cores to ~25x, cap 3 never binding; read
#     `next-batch.py --capacity` before EVERY dispatch, reviewers included,
#     since a reviewer dispatch has no batch call and so never saw the advice.
#   computenet-099p (+16): a same-turn reading bounds only its own turn — one
#     152m boundary landed a session at 313m of a 300m slot, which no reading
#     frequency catches; no forward-looking budget claim from an older reading.
#   computenet-9ofq (+20): the harness "stalled: no progress for 600s"
#     notification named as a distinct case with a fixed response, and the
#     "start acting promptly" re-dispatch line marked as the guess it is.
#   computenet-adgy (+5): PR #650's reviewer asked that agent-execution.md's
#     `bd -C` bullet be generalised to any command line, in the file dispatched
#     agents actually read. It rides here rather than colliding on this entry.
work                RECOMPUTE
# 271 -> 333 on 2026-08-20: the four feedback mechanisms this file is part of
# (recurrence audit, fix_kind, reachability gate, this ratchet) need ~62 lines
# of instruction to be usable. Raised deliberately, after trimming 18 lines of
# rationale out of SKILL.md and into the scripts' own headers, where it is read
# at the point of use instead of loaded into every session.
# 333 -> 346 on 2026-08-29: computenet-z9tu (+13) — the lane's scope line named
# two directories while AGENTS.md gives it all of .claude/skills/, leaving
# sync-report and work-unsupervised editable by nobody (AGENTS.md forbids
# editing a skill outside this lane). Both mechanisms already assumed the wider
# scope, so the prose was the typo. +6 for the reconciliation with the evidence
# that settles which side was wrong, +7 for the NO-MODEL/SERVED contract at
# step 4's reachability call, without which the corrected scope hands sessions
# a tool that returns a false NOT-READ on every non-/work skill file. The
# reachability.py change itself is mechanical and free here.
# 346 -> 353 on 2026-08-29: the same bead's review round (PR #560). The first
# cut had --for answer SERVED for another skill's SKILL.md, which asserts that
# the /work role named on the command line reads it — false, and the l5rc shape
# the gate exists to catch. --for now declines outside /work in every case and
# only the bare form credits a skill's own SKILL.md. +4 to state that split and
# the non-.md decline; +3 for the sentence that would have caught this: the
# scripts have a sibling self-check (feedback.test.sh), it is wired into no
# gate, and this change shipped red for want of running it.
# 353 -> 384 on 2026-08-30: computenet-x69c (+31) — bought step 4's PR-
# concurrency bound. Every PR this lane ships that moves a budget touches this
# very file, so the bound in work/references/direct-child.md (which this lane
# never opens) bites here hardest: six items on 2026-08-29 left five PRs open
# when one merged, and the four raising the same ENTRY went DIRTY while the
# fifth did not. Includes the recompute rule for the conflict, which is not
# mechanically resolvable. +10 of the 31 are the review's: the entry-vs-file
# discriminator the evidence actually shows, the release rule for a hold that
# outlives the session (a claim left in_progress strands the item for 12h),
# and narrowing the script exemption, which is from the ENTRY conflict and not
# from the ~2-per-file bound.
# 384 -> 396 on 2026-09-01: computenet-hkjo (+12) — bought sibling-tests.sh in
# step 4, replacing a sentence that named ONE suite literally. The mechanical
# half (the runner and its own 21-assertion suite) costs no budget lines; the
# 12 are the call, what it selects, and what its two exits mean.
# 396 -> 397 on 2026-09-02: computenet-qae3 (+1) — bought the clause saying a
# merge normally arrives while the NEXT item is already in flight. "On merge:
# ... Repeat from step 2" read literally is a barrier, so the ~2-PR bound two
# lines below it was a dead letter to a literal reader; the 2026-08-29 drain
# ran five PRs at once on the loose reading, and both readings were available.
remediate-friction   397
# 201 -> 267 on 2026-08-29: computenet-d1im — sync-report gains §4, the
# coherence audit: five named checks over a bounded set (top up-next picks,
# beads touched by window decisions, epics a merge may have absorbed), ranked
# into Decisions and recorded as a warning comment on the affected bead. Bought
# with the 2026-08-29 worked example, where reading the backlog against one
# decision found five conflicts in work about to start — including a discovery
# epic whose acceptance forbade the design just chosen. 66 lines against an
# implementation slot spent building the wrong thing; the report had no forward
# check at all before this.
# 267 -> 276 on 2026-08-29: computenet-d1im review round (PR #547). +9 lines
# for the reviewer's one substantive find: the top-of-file contract still said
# bookkeeping drift was THE single exception and that it is "proposed, not
# applied", which contradicted §4 and, carried forward, would have had an agent
# PROPOSE the bead comment instead of writing it — defeating the whole reason
# §4 writes to the bead. The contract now names both exceptions and states that
# they run in opposite directions. Also restores the SOC2 finding (a moderation
# verdict a key rotation silently voided) as the example that the audit catches
# behavioural holes, not only stale wording.
sync-report          276
work-unsupervised     59
