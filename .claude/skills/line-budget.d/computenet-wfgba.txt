AGENTS.md +18
work +11
# The zsh `=word` expansion (`echo ===` -> `(eval):1: == not found`, which also
# kills every command batched AFTER it, so the later half reads as having run
# and produced nothing) stated in the two places dispatched agents actually
# read: AGENTS.md's zsh-trap family beside its six siblings (+18), and
# agent.md's "Running commands" (+11) — the top-level execution rules every
# dispatched agent reads before its role reference.
#
# What the lines buy is PLACEMENT and then SURVIVAL — the two prior fixes failed
# in that order. a49j sat in a file breakdown dispatches are not given (four
# instances after it closed); 6eyp fixed the placement and HELD for a fortnight,
# until the 2026-09-14 distillation (e6ecc3d1) condensed all three copies away.
# Every recurrence since is from the fortnight after — five agents in one slot.
# So: agent.md, which reachability.py scores SERVED at 0 hops for all four
# dispatched roles, and each copy now says in its own text not to delete it.
#
# The mechanical fix — a PreToolUse hook prefixing `setopt noequals`, verified
# by hand — could not be landed here: creating a harness hook and wiring it up
# is refused by the auto-mode self-modification guard. Filed for a human.
