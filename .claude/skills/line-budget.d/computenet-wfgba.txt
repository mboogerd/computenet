AGENTS.md +17
work +11
# The zsh `=word` expansion (`echo ===` -> `(eval):1: == not found`, which also
# kills every command batched AFTER it, so the later half reads as having run
# and produced nothing) stated in the two places dispatched agents actually
# read: AGENTS.md's zsh-trap family beside its six siblings (+17), and
# agent.md's "Running commands" (+11) — the top-level execution rules every
# dispatched agent reads before its role reference.
#
# What the lines buy is SURVIVAL, not wording. Nine-plus instances across
# computenet-a49j, computenet-6eyp, computenet-5hqk1 and this bead. Both prior
# fixes HELD; the 2026-09-14 distillation (e6ecc3d1) condensed all three copies
# away, and every recurrence since is from the fortnight after — five agents in
# one slot. Each copy now says in its own text not to delete it.
#
# The mechanical fix — a PreToolUse hook prefixing `setopt noequals`, verified
# by hand — could not be landed here: creating a harness hook and wiring it up
# is refused by the auto-mode self-modification guard. Filed for a human.
