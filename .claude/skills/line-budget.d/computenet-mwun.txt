work +35

# Thirty-five lines giving section 7 the hand-back section 6 already has, on both
# sides. The reviewer's `bd update --set-metadata review=passed` was DENIED by
# the permission classifier, the same class of denial section 6 documents for
# `git merge` — and section 7 offered no sanctioned move, so the reviewer could
# only leave the verdict half-recorded or exceed its mandate by retrying.
#
# +27 in review-feature.md, +8 in SKILL.md 5e. The SKILL.md half is what review
# insisted on and it is right: section 6's hand-back works because 5e carries
# the other half and calls it the NORMAL path. A reviewer-side instruction with
# no orchestrator-side counterpart rests on the orchestrator reading the
# reviewer's prose, which is the very failure being fixed. It reuses
# feature.md's established REQUIRED ORCHESTRATOR ACTION heading rather than
# inventing a third form, so there is one token to look for.
#
# The same diff corrects two claims review found overstated: `review=passed` is
# not 5e's ship gate (the verdict token is), and 5e is silent on the
# verdict-present/marker-absent direction rather than resolving it — which is
# the gap the SKILL.md half of this same diff closes.
#
# Prose is the only route: an agent cannot script its way around a classifier
# denial, and the remedy is a message to a different actor.
