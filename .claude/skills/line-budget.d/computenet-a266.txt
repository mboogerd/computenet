# computenet-a266: 5b's claim-derivation guidance gains the observed-versus-
# fixed asymmetry, alongside the mechanism-versus-invariant one it already had.
#
# +31 for a shape measured twice — a claim authored from the DEFECT SITES, or
# from the file an acceptance clause names, omits the file where the fix
# belongs whenever the observation site is downstream of the state the fix
# needs — plus the one-hop question that reaches it (which type already holds
# that state?) and the two greps that answer it. Neither check-files-claim.sh
# nor af9q's invariant grep can fire on this shape, which is why prose is the
# only route: both times, the only thing that would have found it was reading
# the two call sites before dispatching. (+1 over the first round: the
# reviewer re-ran both hops at the beads' dispatch-time trees and asked the
# check to name the derivation — the state's noun AS THE BEAD WORDS IT, then
# one hop to the declaring symbol — rather than only the answer symbols, which
# read as "you need something you do not have". Paid for in part by cutting the
# quoted-glob warning, which SKILL.md already gives 230 lines later.)
work +31
