work +22
# wait-checks.sh now bounds every `gh` call, not just total elapsed time: at
# host load1 132 the elapsed stop was never reached, because it is only tested
# between rounds, and the call was auto-backgrounded — no verdict, no table,
# not a reading of the PR at all. SKILL.md step 2 gains the AUTO-BACKGROUNDED
# outcome beside "a failed gh call is not a reading", and review-feature.md
# tells the reviewer that hit it what to do instead of the bare `gh pr checks`
# poll the section forbids. Recurrence of computenet-tl8q, which sized the
# window and stopped there.
