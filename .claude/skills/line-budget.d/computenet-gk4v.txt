# computenet-gk4v: the fact was already in gradle-evidence.md:91-93 (landed
# 297647611, before the bead was filed) — a println goes nowhere because
# testLogging.showStandardStreams is off, and its output is in the XML's
# <system-out>. What was missing was reachability: neither review-feature.md
# nor review-task.md mentioned <system-out>, and both summarise
# gradle-evidence.md as counts + timestamps + --rerun, so a reviewer following
# the pointer never learns the fact exists.
#
# +15 to add <system-out> to those two enumerations, with the "an empty
# console grep is not evidence" clause that is the whole point (+7), and the
# reviewer round (+8): gradle-evidence.md's recipe was `grep -A3` on ONE named
# TEST-<Class>.xml, which truncates the per-seed sweep output that filed this
# bead, so it is now `grep -h -A200 ... *.xml` with the narrow form named as
# a confirm-it-happened check; and "showStandardStreams is off on this build"
# was a false universal — :bench under -PbenchOnly turns it on, and
# review-feature.md section 1 prescribes exactly that run 390 lines above the
# claim. No new content otherwise — gradle-evidence.md is not restated.
work +15
