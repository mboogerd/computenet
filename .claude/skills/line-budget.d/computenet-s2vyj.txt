work +8

# epic.md's dep-wiring example (landed via computenet-zvuu7) was one fenced
# block with a dep() definition and THREE dep calls — the exact shape
# bd-traps.md:404 forbids, and the exact shape whose measured instance
# (computenet-br1y) was DENIED BY THE PERMISSION CLASSIFIER for bundling three
# bd dep add calls. An agent copying the example hits the denial the
# surrounding paragraph exists to prevent. Found by PR #820's reviewer,
# comparing the twin it had just certified in feature.md.
#
# Also drops the real bead ids for placeholders: a copyable snippet of live
# ids invites a verbatim run against the tracker.
#
# +2 from review (PR #822), which FAILED the first attempt: splitting the block
# kept a dep() helper defined in the first call and invoked in later ones —
# and shell state does not survive between Bash calls, so every edge after the
# first would have exited 127 while the transcript read like a run. The $FILT
# family (computenet-adgy) again, reached from the paragraph that warns about
# it. The helper is gone; one `bd dep add` per call, written out, with the
# non-persistence stated so nobody reintroduces it.
