work +20
# Artifact 2 tests module-path disjointness, which is sound for a red in a
# module the change does not touch at all — and has no answer for a
# documentation-and-KDoc diff INSIDE the flaky test's own module, where the
# artifact fails literally while the attribution is airtight (computenet-wugj,
# measured on computenet-zgyt). review-feature.md §7's residual route generates
# that shape routinely, and the honest options left to a reviewer were to report
# an unsatisfied artifact — landing a DRAFT verdict on a provably innocent change
# — or to reason past it silently. The lines name the file-and-causation
# substitute, state the three things a reviewer must SHOW to use it, and require
# an unsatisfied artifact to be reported as unsatisfied rather than either.
