# computenet-nrv5: SKILL.md's scripts table is what an orchestrator calls
# from, and it listed what each script DOES with no arguments at all — every
# call a guess or a re-read of the source.
#
# +8 on work, all of it the paragraph above the table saying what the new first
# column is and that a check keeps it honest. The 21 signatures themselves are
# inline in existing rows and cost no lines. +1 on remediate-friction: one line
# adding the checker to step 4's gate block.
#
# The reviewer's finding that bought the extra lines: sibling-tests.sh triggers
# off `.claude/skills/*/scripts/*`, so an edit to the TABLE — the edit most
# likely to make a row stale — ran no gate at all, and the paragraph claimed
# otherwise. It now names both triggers and says what the check does NOT do
# (it catches a row naming something the script lacks; it cannot catch an
# omitted positional).
#
# The signatures would have drifted the day after they were written — the
# bead's filer said as much ("I have NOT audited the others") — so they land
# with scripts/usage-table.test.sh, which sibling-tests.sh now runs whenever
# any script changes. Scripts are not priced by the ratchet, so the check
# itself is free; this entry buys only the sentence that makes it findable.
work +8
remediate-friction +1
