package civictech.loader.fixture.emptymodule

/**
 * Fixture (c) of epic computenet-051's fixture set: a module jar that declares the
 * manifest attributes but contributes no `ContractModule` service — DISC-05.
 * Plain `kotlin-jvm`, no KSP, no `@Contract`, no `Cell`; this class exists only
 * so the jar is not empty.
 */
class NothingHere
